
int count is 0.
int input.
int total is 0.
int caculateAverage()
{
	out << "Input what you want the average of Below, type 0 to quit "
	while(input != 0)
	{
		cin >> input.
		total is (total + input).
		count is (count + 1).
	}
	out << (total / count).

}